# Formatting Overview

Formatting can be done in Markdown. Or HTML. Or both mixed in the same file.

Pandoc (the tool used to convert the input to HTML and then PDF) supports some extensions to the standard Markdown format.
An overview over Markdown formatting with pandoc can be found at https://pandoc.org/MANUAL.html#pandocs-markdown.

See the following cards for concrete examples.